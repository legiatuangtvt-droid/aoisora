# =============================================================================
# AOISORA BACKEND - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update values according to your environment
#
# FOR LOCAL DEVELOPMENT:
#   1. Copy to .env
#   2. Use LOCAL values below (already set as defaults)
#
# FOR PRODUCTION:
#   1. Copy to .env
#   2. Change APP_ENV=production, APP_DEBUG=false
#   3. Update DB credentials and URLs for production
#   4. Generate new APP_KEY: php artisan key:generate
# =============================================================================

APP_NAME=Aoisora

# Environment: local | production
APP_ENV=local

# IMPORTANT: Generate with: php artisan key:generate
APP_KEY=

# Debug mode: true (local) | false (production)
APP_DEBUG=true

# Base URL
# Local: http://localhost:8000
# Production: https://auraorientalis.vn/api
APP_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_CHANNEL=stack
LOG_LEVEL=error

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
DB_CONNECTION=mysql
DB_PORT=3306
DB_DATABASE=aoisora

# Local settings
DB_HOST=127.0.0.1
DB_USERNAME=root
DB_PASSWORD=

# Production settings (uncomment and update for production):
# DB_HOST=localhost
# DB_USERNAME=auraorie_app
# DB_PASSWORD=your_production_password

# -----------------------------------------------------------------------------
# CACHE & SESSION
# -----------------------------------------------------------------------------
BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# -----------------------------------------------------------------------------
# SANCTUM / CORS
# -----------------------------------------------------------------------------
# Local: localhost:3000,127.0.0.1:3000
# Production: aoisora.auraorientalis.vn
SANCTUM_STATEFUL_DOMAINS=localhost:3000,127.0.0.1:3000

# Local: localhost
# Production: .auraorientalis.vn
SESSION_DOMAIN=localhost

# -----------------------------------------------------------------------------
# MAIL
# -----------------------------------------------------------------------------
# Use 'log' for development (emails logged to storage/logs)
# Use 'smtp' for production with real SMTP server
MAIL_MAILER=log
MAIL_FROM_ADDRESS=noreply@auraorientalis.vn
MAIL_FROM_NAME="${APP_NAME}"

# SMTP settings for production (uncomment and configure):
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your_email@example.com
# MAIL_PASSWORD=your_email_password
# MAIL_ENCRYPTION=tls

# -----------------------------------------------------------------------------
# OPTIONAL: Redis (if using Redis for cache/session)
# -----------------------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# OPTIONAL: WebSocket (Laravel Reverb)
# -----------------------------------------------------------------------------
# REVERB_APP_ID=
# REVERB_APP_KEY=
# REVERB_APP_SECRET=
